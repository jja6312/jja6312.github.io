# Design: OCI CLI Blueprint Engine

Generated by `/office-hours` on 2026-08-22  
Branch: `main`  
Repo: `jja6312/jja6312.github.io`  
Status: **APPROVED**  
Mode: Builder  
Decision owner: 정지안  
Chosen approach: **B. First-class Blueprint Engine**

## 1. Executive decision

현재 OCI CLI 화면에 긴 Bash 레시피를 하나씩 추가하지 않는다. 기존 단일 OCI 명령 카탈로그를 원자 자산으로 유지하고, 그 위에 다음 계층을 추가한다.

1. **Resource CLI**: 현재의 공식 단일 명령과 CRUD/Action
2. **Component**: 함께 배치되는 소규모 리소스 조합
3. **Blueprint**: 입력 계약, 의존성, 검증, Rollback을 포함하는 완성된 구축 패턴
4. **Run Manifest**: 실제 실행 컨텍스트, 생성·재사용된 자원, OCID와 결과 기록
5. **Custom CLI**: 구축 Blueprint가 아닌 운영 절차와 예외 흐름

Blueprint의 정본은 Bash 문자열이 아니라 버전이 있는 선언형 JSON이다. Bash는 해당 정의와 현재 OCI CLI 카탈로그를 결합해 생성되는 산출물이다.

첫 번째 완성 자산은 `network-baseline-2tier/v1`이다. 이 Blueprint는 VCN, Gateway, Route Table, Security List, Regional Subnet을 안전한 순서로 구성한다.

## 2. Problem statement

현재 블로그는 VCN, Subnet, Route Table 같은 단일 리소스 명령을 정확하게 조립할 수 있다. 그러나 고객 구축에서는 다음 문제가 남는다.

- 여러 리소스의 생성 순서를 사용자가 직접 기억해야 한다.
- 앞 명령에서 나온 OCID를 다음 JSON 옵션에 수동으로 옮겨야 한다.
- 같은 고객 환경에 다시 실행했을 때 중복 생성 여부를 판단하기 어렵다.
- 부분 실패 후 어디서부터 재개해야 하는지 기록이 없다.
- Rollback 시 기존 리소스와 이번에 만든 리소스를 구분하기 어렵다.
- Custom CLI가 늘어날수록 특수 분기와 전용 UI가 `CliBuilderPage.tsx`에 누적된다.
- 명령이 동작해도 이것이 어떤 네트워크 표준을 구현한 것인지 버전과 근거가 남지 않는다.

목표는 "CLI 명령 모음"이 아니라 "검증되고 다시 사용할 수 있는 OCI 구축 자산"을 만드는 것이다.

## 3. What makes this useful

사용자는 고객사명, 시스템명, 환경, Compartment, CIDR과 Topology Preset을 입력하거나 JSON 한 블록을 붙여넣는다. 화면은 생성될 이름과 전체 구조를 먼저 보여준다. 사용자는 read-only Discover 스크립트를 실행하고 결과 JSON을 다시 붙여넣는다. 그 뒤에만 `CREATE / REUSE / CONFLICT / BLOCKED` 계획과 Apply 스크립트가 확정된다. Apply가 출력한 Run Result를 다시 가져오면 Provisional Manifest가 만들어지고, 이 Manifest를 기준으로 Verify와 Rollback이 생성된다.

같은 Blueprint를 다른 고객사에 적용할 때 바뀌는 것은 입력값과 Naming Policy뿐이다. 리소스 의존성, 안전장치, 검증, Rollback은 자산에 남는다.

## 4. Current system facts

설계는 다음 현재 구조를 전제로 한다.

- `scripts/generate-cli-catalog.py`가 고정된 OCI CLI Click 트리와 `blog-db/knowledge/oci-cli/_data/*.json`을 결합해 `.protected-cache/cliCatalog.json`을 만든다.
- `scripts/generate-protected-data.mjs`가 CLI 카탈로그를 자물쇠1 보호 데이터 `cliCatalog`로 암호화한다.
- `src/pages/CliBuilderPage.tsx`가 카탈로그 타입, 일반 명령 조립, 특수 Bash 조립, UI 상태를 함께 포함하고 있다.
- `src/lib/cliOptionModel.ts`, `cliExecutionContext.ts`, `cliDynamicLookup.ts`, `cliDefaultOperation.ts`에는 재사용할 수 있는 순수 로직이 이미 있다.
- 즐겨찾기는 브라우저 `localStorage`에 저장되고, 직접 실행 검증 상태는 `blog-db/knowledge/oci-cli/verified.json`에 저장된다.
- 현재 계획에서 `P2-03`은 진행 중이고, `P2-04` Console 맵 분리가 남아 있다.

Blueprint 구현은 서비스 수를 늘리는 Phase 3 전에 공통 자산 계층으로 들어가야 한다. 구현 시 `OCI_CLI_COMPLETION_PLAN.md`에 `Phase 2.5 — Blueprint Foundation`을 추가한다.

## 5. Premises

1. 블로그는 OCI CLI를 직접 실행하지 않는다. 명령을 생성하고, 사용자가 Cloud Shell 또는 로컬 Bash에서 실행한 결과를 다시 받아 해석한다.
2. Blueprint는 OCI 명령 경로와 옵션 메타데이터를 복제하지 않는다. 기존 `cliCatalog.commands[resource].operations[operation]`을 참조한다.
3. 이름만으로는 안전한 네트워크를 만들 수 없다. 최소한 실행 컨텍스트, Address Plan, Topology, 공개 IP 정책은 입력 또는 Preset으로 확정한다.
4. 같은 이름의 자원이 있다는 이유만으로 재사용하지 않는다. 자원별 비교 계약이 일치할 때만 `REUSE`한다.
5. Apply 스크립트는 Discover 결과를 그대로 신뢰하지 않는다. 실행 직전에 같은 조건을 다시 확인한다.
6. Rollback은 Manifest에서 `actualAction: CREATED`이면서 `createdByRun: true`이고, 현재 OCI 자원의 `blueprint-run-id` 관리 태그가 Manifest의 run ID와 일치하는 자원만 역순으로 삭제한다.
7. Generic Blueprint와 Naming Policy는 자물쇠1 자산이다. 고객명·Compartment OCID·실행 결과가 포함된 Manifest는 자물쇠3 또는 로컬 파일로 분리한다.
8. v1은 OCI CLI Bash를 출력한다. Terraform/Resource Manager 출력은 같은 정본을 사용하는 미래 Exporter이며 v1 범위가 아니다.

## 6. Scope

### 6.1 In scope

- Blueprint JSON 스키마와 런타임 타입
- Blueprint 빌드 검증기
- 명령 카탈로그 참조 검증
- Naming Policy와 리소스별 이름 파생
- 의존성 그래프, 순환 참조 검사, 결정적인 실행 순서
- Discover 결과 스키마와 Paste-back 흐름
- Plan 상태 계산
- Apply, Verify, Rollback Bash 생성
- Run Result와 Manifest 생성·Import·Export
- OCI CLI 화면의 독립 `Blueprints` 메뉴
- `network-baseline-2tier/v1`
- 생성 Bash `bash -n`과 대표 시나리오 회귀 테스트
- 보호 데이터 생성, lint, build, 배포, 라이브 확인

### 6.2 Explicit non-goals for v1

- 브라우저나 서버에서 OCI API 직접 호출
- Terraform 코드 생성
- 여러 VCN, Hub-and-Spoke, DRG, LPG, RPC
- IPSec VPN, FastConnect, DNS Resolver 커스텀 구성
- IPv6
- OKE 전용 네트워크
- Load Balancer, Compute Instance, Database 생성
- 기존 리소스의 자동 수정 또는 Route Rule 병합
- 실행 중인 Bash와 브라우저 사이 실시간 스트리밍
- 비밀번호, API Private Key, Auth Token 저장
- Arbitrary shell 또는 JavaScript expression 실행

## 7. Product information architecture

OCI CLI 좌측 패널은 다음 최상위 구분을 갖는다.

1. 즐겨찾기
2. **Blueprints**
3. Custom CLI
4. OCI Console 대분류 기반 Resource CLI

`Blueprints`는 `Custom CLI` 하위에 넣지 않는다. Blueprint는 버전이 있고, 입력·의존성·검증·Manifest 계약을 갖는 정식 자산이다. Custom CLI는 여러 명령을 묶었지만 일반화하지 않은 운영 절차다.

URL은 다음 deep link를 지원한다.

```text
/knowledge/oci-cli?mode=blueprint&blueprint=network-baseline-2tier&version=1
/knowledge/oci-cli?mode=resource&resource=vcn&operation=create
```

잘못된 Blueprint ID나 version은 안전한 기본 Resource 화면으로 조용히 바꾸지 않는다. "해당 Blueprint를 찾을 수 없음"을 표시하고 목록으로 이동시킨다.

## 8. Architecture

```text
blog-db blueprint JSON
        │
        ▼
Blueprint schema validation
        │
        ├── commandRef/option binding validation ──► pinned cliCatalog
        │
        ▼
.protected-cache/cliBlueprintCatalog.json
        │
        ▼
protected-data.json L1: cliBlueprints
        │
        ▼
Blueprint Workspace
   DESIGN → DISCOVER → PLAN → APPLY → RUN RESULT IMPORT
          → PROVISIONAL MANIFEST → VERIFY → FINAL MANIFEST/ROLLBACK
```

### 8.1 Required layers

#### Catalog layer

OCI 공식 명령과 옵션의 단일 기준이다. Blueprint는 이 계층을 읽기만 한다.

#### Definition layer

사용자 입력, Naming Policy, 노드, 의존성, 비교 계약, 결과와 설명을 정의한다.

#### Engine layer

정의와 사용자 입력을 검증하고 이름을 파생하며 DAG를 정렬한다. Discover 결과를 Plan으로 바꾸고 각 Bash 산출물을 만든다.

#### UI layer

입력, 붙여넣기, 상태 표시, Copy/Download/Import를 제공한다. OCI 동작 규칙을 UI 컴포넌트 안에 하드코딩하지 않는다.

#### Persistence layer

Generic 정의는 L1 보호 데이터, 민감할 수 있는 Run Manifest는 L3 또는 로컬 Import/Export로 분리한다.

## 9. Source-of-truth and file ownership

### 9.1 Private source repo: `blog-db`

```text
knowledge/oci-cli/blueprints/
  catalog.json
  network-baseline-2tier.v1.json

knowledge/oci-cli/naming-policies/
  msp-standard.v1.json

knowledge/oci-cli/verified.json
```

`catalog.json`은 노출 순서와 상태만 관리한다. 각 Blueprint 파일이 세부 정본이다.

### 9.2 Public site repo

권장 파일 구조:

```text
schemas/
  oci-cli-blueprint.schema.json
  oci-cli-blueprint-input.schema.json
  oci-cli-artifact-envelope.schema.json
  oci-cli-discovery-result.schema.json
  oci-cli-run-result.schema.json
  oci-cli-verification-result.schema.json
  oci-cli-run-manifest.schema.json

scripts/
  generate-cli-blueprints.mjs
  verify-oci-cli-blueprints.mjs
  verify-oci-cli-blueprint-scripts.mjs

src/lib/oci-cli/
  catalogTypes.ts
  renderOperation.ts
  blueprintTypes.ts
  blueprintCanonical.ts
  blueprintValidate.ts
  blueprintNaming.ts
  blueprintGraph.ts
  blueprintPlan.ts
  blueprintRender.ts
  blueprintManifest.ts

src/pages/cli/
  CliBlueprintWorkspace.tsx
  BlueprintNavigation.tsx
  BlueprintDesignPanel.tsx
  BlueprintDiscoveryPanel.tsx
  BlueprintPlanPanel.tsx
  BlueprintOutputPanel.tsx
  BlueprintManifestPanel.tsx
```

`CliBuilderPage.tsx`는 mode 선택과 기존 Resource 화면 조립을 담당하되 Blueprint 엔진 로직을 직접 담지 않는다.

## 10. Data contracts

### 10.1 Blueprint catalog

```ts
interface CliBlueprintCatalog {
  schemaVersion: 1
  generatedAt: string
  cliSource: {
    repository: string
    tag: string
    version: string
    commit: string
  }
  blueprints: CliBlueprintDefinition[]
  namingPolicies: CliNamingPolicy[]
}
```

### 10.2 Blueprint definition

```ts
interface CliBlueprintDefinition {
  id: string
  version: string
  label: string
  description: string
  category: 'network' | 'compute' | 'database' | 'operations'
  status: 'draft' | 'verified' | 'deprecated'
  namingPolicyId: string
  inputs: BlueprintInputDefinition[]
  presets: BlueprintPreset[]
  nodes: BlueprintNode[]
  outputs: BlueprintOutputDefinition[]
  verification: BlueprintVerificationDefinition[]
  rollback: BlueprintRollbackPolicy
  evidence: BlueprintEvidence
}
```

### 10.3 Typed value source

자유 문자열 `${...}` 평가기는 만들지 않는다. 모든 값은 타입이 있는 참조 객체로 표현한다.

```ts
type BlueprintValueSource =
  | { source: 'literal'; value: unknown }
  | { source: 'input'; input: string }
  | { source: 'derived'; key: BlueprintDerivedKey }
  | { source: 'context'; key: 'profile' | 'region' | 'compartmentId' | 'tenancyId' }
  | { source: 'nodeOutput'; node: string; path: string }
  | { source: 'discovery'; key: string; path: string }
  | { source: 'json'; value: BlueprintJsonValue }
```

`BlueprintJsonValue`도 literal, input, nodeOutput, discovery 참조만 허용한다. 함수 실행, shell substitution, 임의 JavaScript는 금지한다.

JSON 결과 경로는 RFC 6901 JSON Pointer만 허용한다. 임의 JMESPath, JavaScript path, shell expression은 Blueprint source에 넣을 수 없다.

```ts
type BlueprintScalarType = 'string' | 'boolean' | 'number' | 'stringArray' | 'json'

type BlueprintDerivedKey =
  | 'normalizedCustomer'
  | 'normalizedWorkload'
  | 'normalizedEnvironment'
  | 'regionAlias'
  | 'vcnDnsLabel'
  | 'publicSubnetDnsLabel'
  | 'privateSubnetDnsLabel'
  | 'managedFreeformTags'
  | 'publicRouteRules'
  | 'privateRouteRules'
  | 'publicIngressRules'
  | 'publicEgressRules'
  | 'privateIngressRules'
  | 'privateEgressRules'

interface BlueprintNodeOutputDefinition {
  pointer: string
  type: BlueprintScalarType
}
```

v1 derived key registry는 위 union으로 고정한다. node output의 pointer/type은 commandRef별 response-field registry에 존재해야 하며 unknown derived key, output key, pointer 또는 type은 build 실패다.

### 10.4 Composition node

```ts
interface BlueprintNode {
  id: string
  label: string
  commandRef: {
    resource: string
    operation: 'create'
  }
  dependsOn: string[]
  when?: BlueprintCondition
  bindings: Record<string, BlueprintValueSource>
  discovery: BlueprintDiscoveryContract
  comparison: BlueprintComparisonContract
  outputs: Record<string, BlueprintNodeOutputDefinition>
  verify: BlueprintVerifyContract[]
  rollback: BlueprintNodeRollback
}
```

v1에서 Blueprint node의 operation은 `create`로 제한한다. Get/List는 Discover/Verify 계약에서 카탈로그의 공식 동작을 참조한다.

### 10.5 Condition contract

```ts
type BlueprintCondition =
  | {
      kind: 'input'
      input: string
      operator: 'equals' | 'notEquals' | 'in' | 'truthy' | 'falsy'
      value?: string | number | boolean | string[]
    }
  | { kind: 'all' | 'any'; conditions: BlueprintCondition[] }
  | { kind: 'not'; condition: BlueprintCondition }
```

- `equals/notEquals`는 scalar만 비교한다.
- `in`은 Blueprint에 선언된 literal 배열만 사용한다.
- 존재하지 않는 input, 빈 `all/any`, 재귀 깊이 8 초과는 build 실패다.
- condition은 node 활성화와 JSON 배열 항목 포함 여부에만 사용한다. 명령 문자열을 바꾸지 않는다.

### 10.6 Catalog command reference

```ts
type BlueprintCommandRef =
  | { kind: 'resourceOperation'; resource: string; operation: 'get' | 'list' | 'create' | 'delete' }
  | { kind: 'supportCommand'; id: string }
```

`supportCommand`는 Console 메뉴에 노출할 독립 리소스가 없는 read-only 선행 조회용이다. 예를 들어 Oracle Services Network service 목록 조회가 이에 해당할 수 있다. 이 명령도 raw command string을 Blueprint에 적지 않는다. 고정 OCI Click 트리에서 생성·검증된 catalog-level registry에 command path, options, source provenance를 보관하고 Blueprint는 ID만 참조한다. CREATE/UPDATE/DELETE는 support command가 될 수 없다.

### 10.7 Discovery contract

```ts
interface BlueprintDiscoveryContract {
  list: {
    commandRef: BlueprintCommandRef
    bindings: Record<string, BlueprintValueSource>
    pagination: 'all'
    itemsPointer: string
  }
  identity: {
    idPointer: string
    namePointer: string
    expectedName: BlueprintValueSource
    cardinality: 'zero-or-one'
  }
  get?: {
    commandRef: BlueprintCommandRef
    idOption: string
    collect: Record<string, { pointer: string; type: BlueprintScalarType }>
  }
  onError: 'block'
}
```

- `itemsPointer`, `idPointer`, `namePointer`, `collect.pointer`는 RFC 6901 JSON Pointer다.
- pagination 일부 실패, 403/404, schema mismatch를 0건으로 바꾸지 않는다.
- 동일 이름 2건 이상은 `CONFLICT`다.
- `zero-or-one` 이외의 cardinality는 v1에서 지원하지 않는다.

### 10.8 Comparison contract

```ts
interface BlueprintComparisonContract {
  mode: 'exactManagedFields'
  fields: Array<{
    key: string
    desired: BlueprintValueSource
    actualPointer: string
    comparator: 'string' | 'boolean' | 'cidrSet' | 'jsonSet' | 'ocidSet' | 'tagSubset'
    required: true
  }>
}
```

- comparator는 타입별 canonicalization 후 비교한다.
- `jsonSet`은 object key를 정렬하고 array element를 canonical JSON digest로 정렬한다.
- actual field가 없거나 타입이 다르면 일치가 아니라 `CONFLICT`다.
- 관리 태그는 모든 v1 node의 `tagSubset` 비교 필드에 포함한다.
- 비교 계약에 없는 OCI 속성은 v1에서 관리하지 않는다. 다만 managed field에 추가 값이 있으면 exact set 비교에서 `CONFLICT`다.

### 10.9 Verify contract

```ts
interface BlueprintVerifyContract {
  commandRef: BlueprintCommandRef
  bindings: Record<string, BlueprintValueSource>
  assertions: Array<{
    id: string
    actualPointer: string
    comparator: 'equals' | 'containsSet' | 'lifecycleAvailable' | 'tagSubset'
    expected: BlueprintValueSource
    severity: 'fail' | 'warn'
  }>
}
```

Verify는 유효한 Provisional Manifest의 OCID만 입력으로 사용한다. Run Result/Manifest에 없는 node를 이름으로 다시 추측하지 않는다.

### 10.10 Rollback contract

```ts
interface BlueprintNodeRollback {
  commandRef: { kind: 'resourceOperation'; resource: string; operation: 'delete' }
  idOption: string
  waitForState?: string
  ownership: {
    requiredActualAction: 'CREATED'
    runIdTagKey: 'blueprint-run-id'
    requireCurrentTagMatch: true
  }
}
```

Rollback delete binding은 Manifest OCID만 사용한다. 이름 lookup으로 삭제 대상을 새로 찾지 않는다.

### 10.11 Command reference rules

빌드 시 다음 조건이 하나라도 깨지면 보호 데이터를 생성하지 않는다.

- `resource`가 현재 `cliCatalog.commands`에 존재한다.
- 해당 `operation`이 존재한다.
- `bindings`의 모든 옵션이 그 operation의 실제 공개 옵션이다.
- 공식 required 옵션이 binding 또는 안전한 engine context로 충족된다.
- deprecated 옵션은 Blueprint에서 사용할 수 없다.
- nodeOutput 참조 대상이 존재하고 의존성 경로상 앞선 노드다.
- 조건부 노드 참조는 동일하거나 더 강한 조건으로 보호된다.
- DAG에 cycle이 없다.
- node ID, input ID, output ID가 중복되지 않는다.

## 11. Input contract

`network-baseline-2tier/v1` 입력은 다음 그룹으로 구성한다.

### 11.1 Execution context

- `profile`: 기본 `DEFAULT`
- `region`: 필수
- `compartmentMode`: `NAME | OCID`
- `compartment`: 필수
- `auth`: 기존 catalog request context를 재사용

Compartment 이름 조회는 기존 0/1/N 안전 규칙을 재사용한다.

### 11.2 Naming identity

- `customer`
- `workload`
- `environment`: 예 `dev | stg | prd`
- `regionAlias`: Naming Policy에서 기본 파생, 사용자가 override 가능
- `sequence`: 기본 `01`

### 11.3 Address plan

- `vcnCidrs`: v1에서는 IPv4 CIDR 1개
- `publicSubnetCidr`
- `privateSubnetCidr`
- `sshSourceCidr`: SSH ingress를 켰을 때만 필수

### 11.4 Topology

- `preset`: `PUBLIC_PRIVATE_STANDARD`
- `enableSshIngress`: 기본 false

v1의 `PUBLIC_PRIVATE_STANDARD`는 IGW, NAT Gateway, Service Gateway를 모두 생성하고 Public Subnet public IP 허용=true, Private Subnet public IP 허용=false를 강제한다. 이 네 값은 독립 toggle이 아니다. JSON Import에서 false나 다른 조합을 넣으면 조용히 node를 SKIP하지 않고 input validation을 실패시킨다. Gateway 조합을 바꾸는 기능은 별도 versioned Preset으로 추가한다.

### 11.5 Metadata

- Free-form tags
- Defined tags
- `managed-by=oci-cli-blueprint`
- `blueprint-id=network-baseline-2tier`
- `blueprint-version=1`
- `naming-policy-id=msp-standard`
- `naming-policy-version=1`
- Apply에서 CREATE하는 자원에는 `blueprint-run-id=<runId>`

태그 값은 사용자가 끌 수 없는 관리 태그와 사용자 태그를 분리한다. 사용자가 같은 키를 덮어쓸 수 없다.

## 12. Naming Policy

### 12.1 Default pattern

```text
{customer}-{workload}-{environment}-{regionAlias}-{resource}-{role}-{sequence}
```

예시:

```text
acme-erp-prd-se1-vcn-main-01
acme-erp-prd-se1-rt-public-01
acme-erp-prd-se1-subnet-private-01
```

### 12.2 Rules

- 입력 segment는 Unicode 원문과 정규화 결과를 함께 보여준다.
- display name과 DNS label은 별도 파생 규칙을 사용한다.
- DNS label을 단순 문자열 자르기로 만들지 않는다. 길이 축약 후 충돌 가능성을 검사한다.
- 각 리소스의 허용 문자와 길이는 공식 문서 근거와 검증일을 Naming Policy에 기록한다.
- 동일 입력은 항상 동일 이름을 만든다.
- Naming Policy는 `id`, `version`, `digest`를 Manifest에 기록한다.
- 이름 Preview에는 원래 segment, 정규화 결과, 최종 이름, 경고를 표시한다.
- 사용자가 개별 리소스 이름을 override하면 Manifest에 override 사실과 이유 입력란을 남긴다.

## 13. Network Baseline v1

### 13.1 Resources

기본 Preset은 총 10개 자원을 관리한다.

1. VCN
2. Internet Gateway
3. NAT Gateway
4. Service Gateway
5. Public Route Table
6. Private Route Table
7. Public Security List
8. Private Security List
9. Public Regional Subnet
10. Private Regional Subnet

VCN의 기본 DHCP Options ID는 VCN 응답/GET에서 읽어 두 Subnet에 재사용한다. VCN 기본 Route Table과 기본 Security List는 변경하지 않는다.

### 13.2 Dependency graph

```text
VCN
├── Internet Gateway ───────────► Public Route Table ──┐
├── NAT Gateway ───────┐                               │
├── Service Gateway ───┴────────► Private Route Table ─┤
├── Public Security List ──────────────────────────────► Public Subnet
├── Private Security List ─────────────────────────────► Private Subnet
└── Default DHCP Options ──────────────────────────────► Both Subnets
```

### 13.3 Route intent

- Public Route Table: `0.0.0.0/0 → Internet Gateway`
- Private Route Table: `0.0.0.0/0 → NAT Gateway`
- Private Route Table: Oracle Services Network 대상 → Service Gateway

Service Gateway의 service ID와 service CIDR label은 현재 Profile/Region에서 read-only Discover로 조회한다. 하드코딩하지 않는다.

### 13.4 Security baseline

- 모든 ingress는 기본 deny로 시작한다.
- `enableSshIngress=false`가 기본이다.
- 사용자가 SSH ingress를 켜면 `sshSourceCidr`가 필수이며 `0.0.0.0/0`은 경고 또는 정책에 따라 차단한다.
- Public/Private Security List는 egress 정책을 명시적으로 보여준다.
- Private Subnet은 public IP를 허용하지 않는다.
- Public Subnet의 public IP 허용 여부는 화면에서 분명히 표시한다.
- v1은 VNIC에 NSG를 자동 연결하지 않는다. NSG 자산은 후속 Component로 분리한다.

### 13.5 Per-resource comparison contract

| Node | REUSE에 필요한 최소 비교 |
|---|---|
| VCN | compartment, IPv4 CIDR, DNS label, lifecycle state, 관리 태그 subset |
| Gateway | VCN ID, enabled state, gateway 종류, 관리 태그 subset |
| Route Table | VCN ID, 관리 Route Rule의 exact set, 관리 태그 subset |
| Security List | VCN ID, 관리 ingress/egress rule의 exact set, 관리 태그 subset |
| Subnet | compartment, VCN ID, CIDR, regional 여부, route table ID, security list IDs, public IP 정책, DNS label, 관리 태그 subset |

관리 태그 subset에는 `managed-by`, `blueprint-id`, `blueprint-version`, `naming-policy-id`, `naming-policy-version`이 포함된다. 이전 정상 Blueprint run의 `blueprint-run-id` 값은 현재 run과 달라도 되지만 태그 자체는 존재해야 한다. 이름과 설정이 같아도 이 관리 태그가 없으면 `CONFLICT`다. v1은 기존 리소스를 자동 채택하거나 UPDATE하거나 rule을 병합하지 않는다.

## 14. Lifecycle

### 14.1 DESIGN

사용자가 폼 또는 JSON으로 입력한다. 화면은 다음을 즉시 검증한다.

- 필수 입력
- CIDR 형식과 포함 관계
- Subnet 간 중복
- VCN 밖의 Subnet CIDR
- Naming Policy 규칙
- DNS label 충돌
- 조건부 필수값
- 안전하지 않은 공개 정책

### 14.2 DISCOVER

브라우저는 read-only Bash를 생성한다. 사용자가 실행하면 `oci-cli-blueprint-discovery/v1` JSON이 stdout의 명확한 marker 사이에 출력된다.

```text
=== OCI_CLI_BLUEPRINT_DISCOVERY_BEGIN ===
{...json...}
=== OCI_CLI_BLUEPRINT_DISCOVERY_END ===
```

스크립트는 다음을 확인한다.

- OCI CLI와 `jq` 사용 가능 여부
- Profile/Region
- Tenancy/Compartment의 0/1/N 결과
- 같은 display name의 기존 자원 후보 전체
- 후보의 비교에 필요한 GET 결과
- Service Gateway service 목록
- 권한 오류와 pagination 실패

일부 조회 실패를 0건으로 취급하지 않는다. 실패한 node는 `DISCOVERY_ERROR`로 기록한다.

Discover/Apply/Resume/Verify Bash는 marker 사이에 **wire payload만** 출력하며 RFC 8785 digest를 Bash에서 계산하지 않는다. 브라우저와 Node 테스트가 동일한 공용 canonicalization 모듈로 payload를 import envelope로 감싼다.

```ts
interface CliBlueprintArtifactEnvelope<T> {
  envelopeVersion: 1
  artifactType: 'DISCOVERY_RESULT' | 'RUN_RESULT' | 'VERIFICATION_RESULT'
  digest: string
  computedBy: 'blueprint-engine-rfc8785-sha256'
  importedAt: string
  payload: T
}
```

`digest`는 wire payload 전체를 RFC 8785로 canonicalize한 UTF-8 bytes의 SHA-256 lowercase hex다. Discovery payload에는 `generatedAt`, Blueprint/CLI source provenance, resolved profile/auth/tenancy/region/compartment, node별 조회 성공·실패와 원본 비교 field를 모두 포함한다. 같은 자원 상태라도 새 Discover 실행은 `generatedAt` 때문에 새 Plan을 요구하며, Apply 직전 재검증이 별도로 TOCTOU를 방어한다. `jq -S`를 RFC 8785 구현으로 사용하지 않는다.

### 14.3 PLAN

상태는 다음 다섯 가지다.

- `CREATE`: 일치 후보 0건이고 선행 조건이 충족됨
- `REUSE`: 후보가 정확히 1건이고 comparison contract가 일치함
- `CONFLICT`: 중복 후보 또는 설정 불일치
- `BLOCKED`: 의존 node, 입력, 권한, service 조회가 미완성
- `SKIP`: 조건이 꺼진 선택 node

Apply는 모든 활성 node가 `CREATE` 또는 `REUSE`일 때만 활성화한다.

#### Plan Digest contract

Plan Digest는 화면 상태 문자열이 아니라 다음 객체를 RFC 8785 JSON Canonicalization Scheme으로 직렬화한 UTF-8 bytes의 SHA-256 lowercase hex다.

```ts
interface BlueprintPlanDigestPayload {
  schemaVersion: 1
  blueprint: { id: string; version: string; digest: string }
  namingPolicy: { id: string; version: string; digest: string }
  cliSource: { version: string; tag: string; commit: string }
  normalizedInputs: Record<string, unknown>
  context: { profile: string; auth?: string; tenancyId: string; region: string; compartmentId: string }
  discoveryDigest: string
  orderedNodes: Array<{
    id: string
    status: 'CREATE' | 'REUSE' | 'CONFLICT' | 'BLOCKED' | 'SKIP'
    desiredConfigDigest: string
    existingOcid?: string
    existingConfigDigest?: string
  }>
}
```

- Blueprint, Naming Policy, CLI source pin, normalized input, 실행 context, Discovery Result 또는 node 순서가 하나라도 바뀌면 기존 Plan Digest는 즉시 폐기한다.
- Digest 폐기와 함께 이전 Apply/Verify 스크립트, Run Result import, Provisional/Final Manifest도 stale 상태로 바꾸고 복사·실행을 차단한다.
- 브라우저/Node 공용 RFC 8785 모듈이 Plan payload의 canonical bytes와 Digest를 만든다. Apply 스크립트에는 그 canonical bytes의 base64와 Plan Digest를 함께 고정한다.
- Bash는 base64를 임시 파일로 decode한 **동일 bytes**를 `sha256sum`, 없으면 `openssl dgst -sha256`으로 hash할 뿐 JSON을 다시 canonicalize하지 않는다. base64/hash 도구가 없거나 hash가 다르면 변경 없이 중단한다.
- `CONFIRM`은 이 Digest 전체값과 정확히 일치해야 한다. 축약 digest 확인은 허용하지 않는다.

### 14.4 APPLY

Apply 스크립트 요구사항:

- `set -Eeuo pipefail`
- `umask 077`
- `mktemp -d`와 `trap`으로 JSON 임시 파일 정리
- shell array로 request context 전달
- `eval` 금지
- 사용자값과 JSON을 안전하게 quote
- 기본 `MODE=PLAN`
- 실제 변경은 `MODE=APPLY`와 `CONFIRM=<planDigest>`가 모두 맞을 때만 실행
- node 실행 직전에 discovery/comparison 재확인
- CREATE 후 lifecycle wait와 GET 검증
- 실행 전 `runId`와 `attemptId`를 생성하고, 이번 실행이 만든 자원에는 `blueprint-run-id=<runId>` 관리 태그를 생성 요청에 포함
- node별 결과를 Run Result에 누적
- CREATE 직전 재조회에서 동일 자원이 생겼다면 comparison이 정확히 일치할 때만 `REUSED`로 기록하고, 다르면 `CONFLICT_AT_APPLY`로 중단
- `ERR`와 `EXIT` trap은 성공 여부와 무관하게 지금까지 누적된 Run Result를 marker 사이에 출력
- `emit_run_result_once` guard를 사용해 ERR와 EXIT가 연달아 발생해도 Run Result marker는 정확히 한 번만 출력
- 실패하면 이미 성공한 node를 자동 삭제하지 않고, Run Result import를 통해 Resume과 Rollback 산출물을 제공

### 14.5 RUN RESULT IMPORT AND RESUME

Apply stdout marker 사이의 `oci-cli-blueprint-run-result/v1`을 UI에 붙여넣어야 다음 단계로 진행한다.

```ts
interface CliBlueprintRunResultPayload {
  schemaVersion: 1
  runId: string
  attemptId: string
  planDigest: string
  blueprintDigest: string
  contextDigest: string
  inputDigest: string
  startedAt: string
  finishedAt?: string
  status: 'SUCCEEDED' | 'PARTIAL' | 'FAILED'
  nodes: Record<string, BlueprintRunNodeResult>
}

type BlueprintRunNodeResult =
  | {
      status: 'SUCCEEDED'
      name: string
      actualAction: 'CREATED' | 'REUSED'
      ocid: string
      createdByRun: boolean
      attempt: number
      startedAt: string
      finishedAt: string
      configDigest: string
      lifecycleState?: string
    }
  | {
      status: 'FAILED'
      name: string
      attempt: number
      startedAt: string
      finishedAt: string
      error: { code: string; message: string; command?: string }
      resource?: {
        actualAction: 'CREATED' | 'REUSED'
        ocid: string
        createdByRun: boolean
        configDigest?: string
        lifecycleState?: string
      }
      resumeEligible: boolean
      resumeFrom: 'CREATE' | 'WAIT' | 'POST_CREATE_GET'
    }
  | { status: 'PENDING'; name: string; attempt: 0; reason: string }
  | { status: 'SKIPPED'; name: string; attempt: 0; reason: string }

type CliBlueprintRunResult = CliBlueprintArtifactEnvelope<CliBlueprintRunResultPayload>
```

`contextDigest`는 `{ profile, auth, tenancyId, region, compartmentId }`, `inputDigest`는 normalized input의 RFC 8785/SHA-256이다. `runResultDigest`는 Run Result envelope의 `digest`, `verificationResultDigest`는 Verification Result envelope의 `digest`를 뜻한다. 모든 imported artifact digest는 공용 브라우저/Node 모듈이 wire payload만 대상으로 계산한다. Manifest `digest`는 Manifest 객체에서 `digest` field만 제거한 payload를 같은 방식으로 계산한다.

SUCCEEDED node와 FAILED node의 `resource` 모두에서 `actualAction: CREATED`와 `createdByRun: true`는 동치여야 한다. `REUSED`인데 `createdByRun: true`인 결과처럼 모순된 import는 schema가 통과해도 semantic validation에서 거절한다.

- `resumeFrom=CREATE`면 `resource`가 없어야 한다.
- `resource`가 있으면 `resumeFrom`은 `WAIT | POST_CREATE_GET`이어야 하며 Create 명령을 다시 실행할 수 없다.
- CREATE API 응답에서 OCID를 얻은 직후, wait 전에 Run Result에 `resource.actualAction=CREATED`, `createdByRun=true`, `ocid`를 먼저 flush한다.

Run Result import가 유효하면 먼저 Provisional Manifest를 만든다. 부분 실패 상태에서도 이미 생성한 자원의 소유권과 Rollback Preview는 보존한다.

Resume 계약:

- 동일 `runId`를 유지하고 새 `attemptId`를 생성한다. node의 `attempt`만 증가한다.
- Blueprint/Plan/Context Digest가 모두 같고, Provisional Manifest와 Run Result가 유효할 때만 Resume을 생성한다.
- 이전 `SUCCEEDED` node는 OCID로 다시 GET하고 comparison contract가 여전히 일치할 때 실행을 건너뛴다.
- 이전 `FAILED` 중 `resumeEligible=true`와 `PENDING` node만 DAG 순서로 재시도한다.
- FAILED node에 `resource.actualAction=CREATED`가 있으면 같은 `runId` 태그를 GET으로 확인하고 CREATE를 다시 실행하지 않는다. `resumeFrom`에 따라 WAIT 또는 POST_CREATE_GET부터 재개하며 소유권을 유지한다.
- 이전 `CREATED` node는 새 시도에서도 소유권이 같은 `runId`에 남고, `REUSED` node는 계속 비소유 상태다.
- 기존 성공 node에 drift, 삭제 또는 소유권 태그 불일치가 있으면 `RESUME_BLOCKED`로 중단한다.
- Resume도 `ERR/EXIT` trap에서 전체 누적 Run Result를 marker 사이에 출력한다.

### 14.6 VERIFY

Verify 스크립트는 read-only다.

- 완전한 Run Result에서 생성된 유효한 Provisional Manifest가 있어야 생성할 수 있다.
- node 조회는 Provisional Manifest의 OCID만 사용한다. 이름으로 다시 찾지 않는다.
- 부분 실패 Run Result는 성공 node의 Rollback Preview까지만 허용하고 전체 Verify는 비활성화한다.

- 모든 node OCID 존재
- lifecycle state 정상
- Subnet이 올바른 VCN/RT/SL/DHCP를 참조
- Route Rule target이 기대 Gateway와 일치
- Private Subnet public IP 정책 확인
- 관리 태그와 Blueprint version 확인

검증 결과는 `PASS | WARN | FAIL | ERROR`로 node별 출력한다.

```ts
interface CliBlueprintVerificationResultPayload {
  schemaVersion: 1
  runId: string
  planDigest: string
  runResultDigest: string
  provisionalManifestDigest: string
  startedAt: string
  finishedAt: string
  status: 'PASS' | 'WARN' | 'FAIL' | 'ERROR'
  nodes: Record<string, {
    ocid: string
    status: 'PASS' | 'WARN' | 'FAIL' | 'ERROR'
    assertions: Array<{
      id: string
      status: 'PASS' | 'WARN' | 'FAIL' | 'ERROR'
      message: string
      expected?: unknown
      actual?: unknown
    }>
  }>
}

type CliBlueprintVerificationResult = CliBlueprintArtifactEnvelope<CliBlueprintVerificationResultPayload>
```

Verify 결과 JSON을 UI에 다시 붙여넣으면 Provisional Manifest와 결합해 Final Manifest를 만든다. Run Result나 Provisional Manifest Digest가 다르면 merge하지 않는다.

### 14.7 MANIFEST

Run Result를 UI에 붙여넣으면 Provisional `oci-cli-blueprint-manifest/v1`이 만들어지고, Verify 결과까지 결합하면 Final Manifest가 된다.

```ts
interface CliBlueprintRunManifest {
  schemaVersion: 1
  digest: string
  phase: 'PROVISIONAL' | 'FINAL'
  status: 'SUCCEEDED' | 'PARTIAL' | 'FAILED'
  runId: string
  blueprint: { id: string; version: string; digest: string }
  namingPolicy: { id: string; version: string; digest: string }
  cliSource: { version: string; tag: string; commit: string }
  createdAt: string
  context: {
    profile: string
    auth?: string
    tenancyId: string
    region: string
    compartmentId: string
  }
  inputDigest: string
  discoveryDigest: string
  planDigest: string
  runResultDigest: string
  inputs: Record<string, unknown>
  nodes: Record<string, BlueprintManifestNode>
  verification?: CliBlueprintVerificationResult
  warnings: string[]
}

type BlueprintManifestNode =
  | {
      status: 'SUCCEEDED'
      name: string
      ocid: string
      actualAction: 'CREATED' | 'REUSED'
      createdByRun: boolean
      configDigest: string
      lifecycleState?: string
    }
  | {
      status: 'FAILED'
      name: string
      error: { code: string; message: string }
      resource?: {
        actualAction: 'CREATED' | 'REUSED'
        ocid: string
        createdByRun: boolean
        configDigest?: string
        lifecycleState?: string
      }
      resumeEligible: boolean
      resumeFrom: 'CREATE' | 'WAIT' | 'POST_CREATE_GET'
    }
  | { status: 'PENDING' | 'SKIPPED'; name: string; reason: string }
```

Manifest에는 Secret, API Private Key, Security Token을 넣지 않는다.

`phase=PROVISIONAL`이면 `verification`은 없어야 하고, `phase=FINAL`이면 digest가 일치하는 단일 `CliBlueprintVerificationResult`가 필수다. Verification Result는 node assertion들의 단일 실행 묶음이므로 배열로 저장하지 않는다.

### 14.8 ROLLBACK

- Manifest가 없으면 Rollback을 생성하지 않는다.
- `blueprint.digest`, `runId`, `compartmentId`를 확인한다.
- node 성공 여부와 무관하게 node 자체 또는 FAILED node의 `resource`에 `actualAction=CREATED`, `createdByRun=true`, 유효한 `ocid`가 모두 기록돼 있어야 후보가 된다.
- CREATE API 성공 후 WAIT/GET이 실패한 node도 소유권이 사라지지 않으며 Rollback Preview에 포함한다.
- 삭제 직전 현재 자원을 GET하여 `blueprint-run-id` 태그가 Manifest의 `runId`와 정확히 일치하는지 다시 확인한다.
- Manifest JSON의 boolean만 수동으로 바꿔도 현재 태그가 일치하지 않으면 삭제할 수 없다.
- DAG의 역순으로 삭제한다.
- `REUSED` node는 항상 SKIP한다.
- 기본은 Preview다.
- 실제 삭제는 `MODE=ROLLBACK`, `CONFIRM_RUN_ID`, `CONFIRM_COMPARTMENT_ID`가 모두 일치할 때만 수행한다.
- 현재 자원 상태가 Manifest와 다르면 삭제하지 않고 `DRIFT_BLOCKED`로 멈춘다.
- 삭제 불가 자원은 다음 수동 조치와 남은 자원 목록을 출력한다.

## 15. UI design

### 15.1 Workspace tabs

```text
DESIGN | DISCOVER | PLAN | APPLY | VERIFY | MANIFEST
```

Rollback은 MANIFEST 안의 명시적인 위험 작업 영역에 둔다.

### 15.2 DESIGN view

- Blueprint/Preset 선택
- JSON 붙여넣기와 현재 폼을 JSON으로 복사
- Execution, Naming, Address Plan, Topology, Security, Tags 섹션
- 생성될 10개 이름 Preview
- 간단한 네트워크 구조도

v1 Import 형식은 JSON만 지원한다. YAML parser는 추가하지 않는다.

### 15.3 DISCOVER view

- read-only임을 분명히 표시
- 스크립트 복사
- 결과 JSON 붙여넣기
- marker 밖 로그가 있어도 marker 사이 JSON만 추출
- schema/version/digest 검증

### 15.4 PLAN view

각 node를 실행 순서대로 표시한다.

```text
CREATE   acme-erp-prd-se1-vcn-main-01
REUSE    acme-erp-prd-se1-igw-main-01
CONFLICT acme-erp-prd-se1-rt-private-01
```

CONFLICT를 클릭하면 기대값과 발견값의 차이를 보여준다. OCID 전체는 복사 가능하되 기본 표시는 축약한다.

### 15.5 APPLY/VERIFY view

- 기존 최종 명령 패널의 Alt+O, Alt+C 동작을 유지
- 최초 펼침 높이는 화면 절반 이하
- 다시 펼치면 전체 높이 허용
- Download `.sh` 제공
- CI fixture에서 renderer template가 `bash -n`을 통과했다는 `빌드 검증된 템플릿` badge 표시
- 브라우저는 사용자별로 생성된 Bash를 실제 `bash -n` 실행했다고 주장하지 않는다.
- Apply는 유효한 Plan Digest가 없으면 복사 불가
- Apply 아래에 Run Result import panel과 Resume action을 제공
- 유효한 Provisional Manifest가 생기기 전 Verify 복사 불가
- 입력/Context/Discovery가 바뀌어 Digest가 폐기되면 Apply, Run Result, Verify, Manifest를 stale로 표시하고 재생성 안내

### 15.6 MANIFEST view

- Run Result 붙여넣기
- Provisional Manifest Preview
- Verify Result 붙여넣기와 Final Manifest Preview
- JSON Download/Import
- 로컬 Draft 저장
- PAT가 있으면 L3 저장을 선택적으로 제공할 수 있으나 v1 필수 완료 조건은 Download/Import다.

### 15.7 Right validation sidebar

현재 `실행 전 입력 확인` 패널을 재사용하고 다음 범주를 구분한다.

- INPUT
- NAMING
- NETWORK
- DISCOVERY
- CONFLICT
- SAFETY

문제를 클릭하면 해당 필드 또는 Plan node에 focus한다.

### 15.8 Responsive behavior

- 1100px 이하에서는 우측 검증 패널을 본문 아래로 이동한다.
- 760px 이하에서는 좌측 메뉴를 drawer로 전환한다.
- sticky 영역이 본문과 겹치지 않는다.
- 긴 Bash, 긴 OCID, 47자 이상의 이름이 가로 overflow를 만들지 않는다.
- 모바일에서도 DESIGN부터 MANIFEST까지 순차 접근 가능해야 한다.

## 16. Persistence and protection

### 16.1 L1 generic assets

`ProtectedBundle`에 추가한다.

```ts
cliBlueprints?: CliBlueprintCatalog
```

Blueprint 정의와 Naming Policy에는 실 고객명, 실제 OCID, IP, Secret을 넣지 않는다.

### 16.2 Local draft

```text
hub-cli-blueprint-session-v1
hub-cli-blueprint-drafts-v1   # 명시적 장기 저장 opt-in 전용
```

저장 내용:

- blueprint ID/version
- 입력 폼
- 마지막 discovery digest와 Plan 요약은 민감 가능성을 경고한 뒤 명시적 장기 저장에서만 포함

- 자동 복구는 동일 탭의 `sessionStorage`에만 저장하고 30일 TTL, 저장 시각, schema version을 기록한다.
- `localStorage` 장기 저장은 사용자가 `이 브라우저에 30일 보관`을 직접 켠 경우에만 사용한다.
- Discovery raw JSON, Run Result, Provisional/Final Manifest는 session/local storage에 자동 저장하지 않는다.
- `이 기기가 공유 PC라면 저장하지 마세요` 경고와 `현재 Blueprint 데이터 모두 지우기`를 항상 제공한다.
- TTL 만료, schema mismatch, 사용자 Clear 시 session/local key를 함께 제거한다.
- 실제 Run Manifest 저장은 사용자가 명시적으로 Download를 선택한 경우만 v1에서 지원한다.
- 새로고침 후 자동 복구되는 것은 입력 draft뿐이다. 복구된 Discovery digest/Plan summary는 항상 `STALE_REFERENCE`로 표시하며 Discover를 다시 실행하거나 결과를 재Import하기 전에는 Apply에 사용할 수 없다.

### 16.3 L3 run history

후속 단계에서 `blog-db/knowledge/oci-cli/runs/*.json` 저장을 지원한다. PAT가 있을 때만 write하며, 자물쇠3 비밀번호만 있는 사용자는 기존처럼 read-only다. v1 배포를 이 기능 때문에 막지 않는다.

## 17. Refactoring boundary

`CliBuilderPage.tsx`의 모든 로직을 한 번에 재작성하지 않는다. 다음 순서로 최소한만 추출한다.

1. 카탈로그 인터페이스를 `src/lib/oci-cli/catalogTypes.ts`로 이동
2. 일반 공식 명령 직렬화를 `renderOperation.ts`의 순수 함수로 이동
3. 기존 Resource UI가 새 순수 함수를 사용하도록 회귀 보호
4. Blueprint renderer도 같은 함수를 사용
5. Cross Copy, Cleanup 같은 특수 builder는 v1에서 그대로 둠

리팩터링 후 기존 219개 대표 생성 명령과 동적 조회 테스트가 그대로 통과해야 한다.

## 18. Build pipeline

`gen:protected` 순서를 다음처럼 확장한다.

```text
generate-cli-catalog.py
→ existing OCI metadata/lookups/commands gates
→ generate-cli-blueprints.mjs
→ verify-oci-cli-blueprints.mjs
→ verify-oci-cli-blueprint-scripts.mjs
→ generate-protected-data.mjs
```

Blueprint generator는 `.protected-cache/cliCatalog.json`과 `blog-db` 정의를 함께 읽는다. 따라서 OCI commandRef와 option binding이 현재 고정 Click 트리에 없으면 생성 단계에서 실패한다.

## 19. Failure handling

| Failure | Required behavior |
|---|---|
| Compartment 0건/중복 | PLAN 전에 중단하고 후보/입력 수정 안내 |
| 권한 404/403 | 0건으로 취급하지 않고 DISCOVERY_ERROR |
| pagination 일부 실패 | 전체 조회 실패로 처리 |
| 동일 이름 2개 이상 | CONFLICT |
| 동일 이름, 설정 불일치 | CONFLICT, 자동 UPDATE 금지 |
| 관리 태그 없는 동일 이름 자원 | CONFLICT, v1 자동 adopt 금지 |
| Plan 입력·Context·Discovery 변경 | 기존 Digest와 downstream 산출물 즉시 무효화 |
| Apply 중 node 실패 | ERR/EXIT trap으로 누적 Run Result 출력, 성공 node 보존 |
| CREATE 성공 후 WAIT/GET 실패 | FAILED.resource에 CREATED ownership/OCID 보존, Resume과 Rollback Preview에 포함 |
| Run Result 부분 실패 | Provisional Manifest와 Rollback Preview 제공, 전체 Verify 차단 |
| Resume 전 성공 node drift | RESUME_BLOCKED, 변경 명령 실행 금지 |
| 출력 JSON 손상 | marker와 schema 검증 실패 표시 |
| Blueprint version mismatch | 자동 변환 금지, migration 필요 표시 |
| OCI CLI source pin 변경 | blueprint commandRef 재검증 필수 |
| Rollback ownership tag 불일치 | 해당 node 삭제 금지 |
| Rollback 중 drift | 해당 node 삭제 금지 |
| 브라우저 새로고침 | 입력 draft만 복구, Discover 재실행/재Import 전 Plan은 실행 불가 |

## 20. Test strategy

### 20.1 Schema and generator

- 정상 Blueprint 생성
- unknown resource/operation/option 실패
- required binding 누락 실패
- deprecated option 사용 실패
- node/input/output 중복 실패
- cycle 실패
- 조건부 node의 안전하지 않은 참조 실패
- source pin과 Blueprint provenance 기록
- unknown derived key/output pointer/type 실패
- Node/browser가 동일 wire payload에 같은 RFC 8785 canonical bytes와 digest 생성

### 20.2 Naming

- 동일 입력의 결정적 결과
- 대소문자, 공백, 한글, 특수문자 정규화
- 긴 segment 축약
- DNS label 규칙
- 축약 후 충돌
- individual override 기록
- region alias 미정의 처리

### 20.3 Network validation

- 올바른 VCN/Subnet CIDR
- Subnet overlap
- VCN 밖 Subnet
- Public/Private 동일 CIDR
- SSH on + source 없음
- SSH source `0.0.0.0/0`
- private public IP 강제 false

### 20.4 Plan fixtures

1. 빈 Compartment: 10개 CREATE
2. 완전 일치: 10개 REUSE
3. 부분 존재: CREATE/REUSE 혼합
4. 중복 이름: CONFLICT
5. CIDR mismatch: CONFLICT
6. discovery permission error: BLOCKED
7. 관리 태그 없는 동일 이름 자원: CONFLICT
8. v1 고정 Topology와 다른 Import 조합: 입력 validation 실패
9. input/context/discovery 변경: 기존 Plan Digest와 downstream 산출물 무효화

### 20.5 Bash contracts

- DISCOVER/APPLY/RESUME/VERIFY/ROLLBACK 모두 `bash -n`
- `eval` 문자열 없음
- unchecked `data[0]` 없음
- 기본 실행에서 변경 명령 없음
- APPLY confirm digest 불일치 시 변경 없음
- rollback에서 REUSED 삭제 명령 없음
- rollback에서 `CREATED + createdByRun + 현재 run-id tag` 중 하나라도 불일치하면 삭제 없음
- rollback에서 CREATE 성공 후 WAIT/GET 실패한 owned resource는 현재 run-id tag가 일치하면 Preview 대상
- Apply/Resume의 ERR·EXIT 경로에서도 유효한 누적 Run Result marker 출력
- ERR 뒤 EXIT가 이어져도 `emit_run_result_once`로 marker 묶음은 정확히 1회 출력
- Resume은 성공 node를 재검증하고 FAILED/PENDING만 DAG 순서로 재시도
- CREATE 성공 후 WAIT/GET 실패 Run Result가 CREATED ownership과 OCID를 보존
- 위 FAILED node Resume이 CREATE를 반복하지 않고 WAIT/POST_CREATE_GET부터 재개
- special characters가 shell injection을 만들지 않음
- JSON은 임시 파일 또는 안전한 `jq -n`으로 전달

### 20.6 Run Result, Manifest, persistence

- SUCCEEDED/FAILED/PENDING/SKIPPED discriminated union schema
- `CREATED ↔ createdByRun=true`, `REUSED ↔ createdByRun=false` semantic invariant
- FAILED.resource의 action/ownership invariant와 resumeFrom 조합 검증
- 부분 실패 Run Result → Provisional Manifest, Rollback Preview, Verify disabled
- Run Result + Verify Result digest 일치 → Final Manifest
- digest mismatch 결과 merge 거절
- session draft TTL/clear/schema mismatch 동작
- 새로고침 복구 후 Plan summary는 stale/non-actionable, Discover 재실행/재Import 필요
- Discovery raw/Run Result/Manifest 자동 저장 금지
- UI badge는 `빌드 검증된 템플릿`만 표현하고 사용자별 Bash 검증으로 오인시키지 않음

### 20.7 Regression

- 기존 `npm run test:oci-*` 전체 통과
- 기존 Resource CLI generated command 결과 유지
- 즐겨찾기·CRUD/Action 검증 상태 유지
- Alt+O/Alt+C 유지
- 데스크톱/모바일 UI 확인
- 보호 데이터 L1/L2/L3 복호화 검증

## 21. Acceptance criteria

다음을 모두 만족해야 v1 완료다.

1. JSON 한 블록을 붙여넣으면 2-Tier Blueprint 입력과 이름 Preview가 채워진다.
2. 실제 OCI 변경 전 read-only Discover 스크립트를 얻는다.
3. Discover 결과 없이 Apply를 복사할 수 없다.
4. Plan은 모든 node를 CREATE/REUSE/CONFLICT/BLOCKED/SKIP으로 표시한다.
5. 같은 입력과 완전 일치 환경에서 재실행해 중복 자원을 만들지 않는다.
6. 같은 이름과 다른 CIDR 환경에서 자동 재사용 또는 UPDATE하지 않는다.
7. Apply가 부분 실패해도 생성된 자원과 재사용 자원을 구분한 Run Result를 남긴다.
   CREATE API 성공 뒤 WAIT/GET이 실패한 경우도 OCID와 CREATED 소유권을 잃지 않는다.
8. Run Result import가 Provisional Manifest를 만들고, 부분 실패 시 Verify를 막으면서 Rollback Preview를 보존한다.
9. Verify가 Provisional Manifest OCID만 사용해 VCN, Gateway, RT, SL, Subnet 연결을 read-only로 확인한다.
10. Verify Result가 같은 digest의 Provisional Manifest와 결합돼 Final Manifest를 만든다.
11. Rollback은 `CREATED + createdByRun + 현재 run-id tag 일치` 자원만 역순 삭제하도록 생성된다.
12. 입력·Context·Discovery가 바뀌면 이전 Plan과 downstream 산출물이 즉시 무효화된다.
13. Blueprint 정의는 고정 OCI CLI 카탈로그의 실제 공개 option만 사용한다.
14. 모든 renderer template와 대표 생성 Bash fixture가 `bash -n`을 통과한다.
15. 보호 데이터 생성, OCI 전용 테스트, lint, build가 통과한다.
16. GitHub Pages 배포 후 실제 사이트에서 데스크톱과 모바일 흐름을 확인한다.
17. 라이브 JS, CSS, protected-data가 로컬 최종 산출물과 일치한다.

## 22. Implementation phases

### Phase 2.5-01: Contracts and generator

- schema, types, source files
- commandRef binding validation
- protected L1 pipeline

### Phase 2.5-02: Pure engine

- naming, DAG, input validation, plan
- discovery/run-result/verification/manifest parsers
- Plan Digest, Resume, Provisional/Final Manifest merge
- Discover/Apply/Resume/Verify/Rollback Bash renderers

### Phase 2.5-03: Network Blueprint

- 2-Tier definition
- Service Gateway discovery
- comparison/verify/rollback contracts

### Phase 2.5-04: UI

- Blueprint nav/workspace
- DESIGN/DISCOVER/PLAN/APPLY/VERIFY/MANIFEST
- JSON Import/Export, draft
- validation focus and responsive layout

### Phase 2.5-05: Release

- tests, lint, build
- completion plan evidence
- commit, push, deploy
- live artifact and mobile verification

각 Phase는 테스트가 통과해야 다음으로 진행한다. 체크박스는 전체 완료·배포·라이브 검증 전까지 완료 처리하지 않는다.

## 23. Approaches considered

### A. Network Bash Bundle

빠르지만 새 Blueprint마다 전용 분기와 스크립트가 늘어난다. 자산의 입력·버전·Manifest 계약이 남지 않는다.

### B. First-class Blueprint Engine

기존 카탈로그를 재사용하면서 구축 자산을 일반화한다. 첫 구현량은 더 크지만 이후 Compute, DB, OKE, Monitoring 조합이 같은 엔진 위에 올라간다.

### C. Terraform/Resource Manager as canonical source

상태와 drift 관리에는 가장 강하지만 사용자가 현재 원하는 "OCI CLI 복붙 자산" 경험을 바꾼다. v1 이후 exporter로 검토한다.

## 24. Recommended approach

B를 구현한다. 단, 첫 배포는 범용 엔진과 `network-baseline-2tier/v1` 하나를 end-to-end로 완성하는 데 제한한다. 여러 Blueprint를 얕게 추가하지 않는다.

## 25. Open decisions with defaults

설계를 막는 미결정은 없다. 구현자는 아래 기본 결정을 따른다.

| Topic | Default |
|---|---|
| Import format | JSON only |
| Browser OCI execution | 하지 않음 |
| Existing resource mismatch | CONFLICT, no auto-update |
| Manifest v1 persistence | Download/Import required, L3 remote save deferred |
| Security | explicit Security Lists, ingress deny by default |
| NSG | 후속 Component |
| IPv6 | deferred |
| Terraform exporter | deferred |
| First preset | Public + Private regional 2-tier |

## 26. Distribution plan

기존 GitHub Pages 배포 경로를 그대로 사용한다. Blueprint source는 private `blog-db`, 생성된 암호화 catalog만 public site에 반영한다. 구현 완료 시 기존 자동 배포를 실행하고 라이브 사이트에서 확인한다.

## 27. The assignment

Claude에게 동봉된 구현 프롬프트와 이 설계 문서를 전달한다. 배포 후에는 비운영 테스트 Compartment에서 다음 한 건을 직접 검증한다.

- 임의 고객/워크로드명
- VCN `/16`
- Public/Private `/24`
- Discover → Plan → Apply → Run Result Import → Provisional Manifest → Verify → Final Manifest → Rollback Preview

실제 Rollback 실행은 생성 자원이 테스트용임을 확인한 뒤에만 수행한다.

## 28. What I noticed about how you think

- 사용자는 "명령 하나 추가"보다 "이름과 네이밍 컨벤션만 정해서 복붙하면 기본 구조가 만들어지는 자산"을 원했다. 재사용 단위를 명령이 아니라 구축 패턴으로 올리려는 요구다.
- 사용자는 구현 전에 설계를 분리하고, 설계자와 구현자를 나눴다. 이 문서는 구현자가 다시 제품 방향을 추측하지 않도록 결정과 비결정을 구분한다.
- 과거 OCI CLI 수정에서 빠진 동적 조회, 잘못된 메뉴 위치, 연관 기능 누락을 반복해서 지적했다. 그래서 이 설계는 commandRef 검증, 닫힌 lifecycle, Plan/Manifest를 완료 조건으로 둔다.
